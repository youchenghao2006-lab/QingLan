# 参与贡献

欢迎反馈问题、改进可访问性、完善界面和补充测试。提交贡献表示你有权按本仓库 MIT 许可提供该贡献；第三方代码和素材请注明来源与许可，不要提交来路不明的文章、声音、字体或图片。

## 开发约定

- 业务规则与存储放在 `src/QingLan/Core`，界面放在 `Views`，可复用视觉组件放在 `Controls`。
- 尽量保持修改聚焦。界面改动检查窗口化、最大化、字体缩放和键盘操作。
- 存储修改应覆盖失败保存、重启加载和删除后不再出现的测试。
- 使用独立的 `QINGLAN_DATA_DIR`，不要对真实用户数据运行实验性迁移。
- 先运行 `scripts/Test.ps1`，再运行 `scripts/Publish.ps1`；测试不代替实际界面检查。

## 报告问题

提供 Windows 版本、.NET SDK 版本、复现步骤、期望与实际结果。日志与截图先脱敏，不附带随心记、姓名、真实课表、令牌或整个数据目录。

若发现可能泄露私人数据的安全问题，请勿在公开 issue 中粘贴可利用细节或敏感文件，应使用维护者后续在仓库设置的私密安全报告渠道。
